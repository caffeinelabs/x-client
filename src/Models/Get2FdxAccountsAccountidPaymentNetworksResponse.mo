
import { type PlaidAccountPaymentNetwork; JSON = PlaidAccountPaymentNetwork } "./PlaidAccountPaymentNetwork";

import { type Problem; JSON = Problem } "./Problem";
import { Candid } "mo:serde-core";
import Array "mo:core/Array";
import List "mo:core/List";
import Float "mo:core/Float";

// Get2FdxAccountsAccountidPaymentNetworksResponse.mo

module {
    public type Get2FdxAccountsAccountidPaymentNetworksResponse = {
        data : ?[PlaidAccountPaymentNetwork];
        errors : ?[Problem];
    };

    public module JSON {
        public func toCandidValue(value : Get2FdxAccountsAccountidPaymentNetworksResponse) : Candid.Candid {
            let buf = List.empty<(Text, Candid.Candid)>();
            switch (value.data) {
                case (?v__) List.add(buf, ("data", #Array(Array.map<PlaidAccountPaymentNetwork, Candid.Candid>(v__, PlaidAccountPaymentNetwork.toCandidValue))));
                case null ();
            };
            switch (value.errors) {
                case (?v__) List.add(buf, ("errors", #Array(Array.map<Problem, Candid.Candid>(v__, Problem.toCandidValue))));
                case null ();
            };
            #Record(List.toArray(buf));
        };

        public func fromCandidValue(candid : Candid.Candid) : ?Get2FdxAccountsAccountidPaymentNetworksResponse =
            switch (candid) {
                case (#Record(fields)) {
                    let data : ?[PlaidAccountPaymentNetwork] = switch (Array.find<(Text, Candid.Candid)>(fields, func((k, _) : (Text, Candid.Candid)) : Bool = k == "data")) {
                        case (?data_field) ((switch (data_field.1) {
                        case (#Array(xs__)) {
                            let buf__ = List.empty<PlaidAccountPaymentNetwork>();
                            for (c__ in xs__.values()) {
                                let ?m__ = PlaidAccountPaymentNetwork.fromCandidValue(c__) else return null;
                                List.add(buf__, m__);
                            };
                            ?List.toArray(buf__);
                        };
                        case _ null;
                    }));
                        case null null;
                    };
                    let errors : ?[Problem] = switch (Array.find<(Text, Candid.Candid)>(fields, func((k, _) : (Text, Candid.Candid)) : Bool = k == "errors")) {
                        case (?errors_field) ((switch (errors_field.1) {
                        case (#Array(xs__)) {
                            let buf__ = List.empty<Problem>();
                            for (c__ in xs__.values()) {
                                let ?m__ = Problem.fromCandidValue(c__) else return null;
                                List.add(buf__, m__);
                            };
                            ?List.toArray(buf__);
                        };
                        case _ null;
                    }));
                        case null null;
                    };
                    ?{
                        data;
                        errors;
                    };
                };
                case _ null;
            };
    };
};
